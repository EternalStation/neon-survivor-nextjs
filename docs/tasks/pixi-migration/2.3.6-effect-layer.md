# Task 2.3.6 — Effect Layer

## Status: Not Started

## Dependencies: 2.1, 2.2

## Goal
Migrate area effects and floating numbers from `EffectRenderer.ts` (1,307 lines) to PixiJS.

## Current Implementation
- Puddles drawn with filled circles + alpha
- Epicenter drawn with radial gradients
- Glitch clouds drawn with noise patterns
- Floating numbers drawn with `ctx.fillText()`
- Black hole effects drawn with composite operations

## PixiJS Implementation
- Puddles -> `Sprite` with tint + alpha animation
- Epicenter -> radial gradient texture + scale animation
- Glitch clouds -> noise texture + alpha mask
- Floating numbers -> `BitmapText` (pre-rendered font atlas)
- Black hole -> custom effect with blend modes

## Source File
- `EffectRenderer.ts` — 1,307 lines

## Layer
- `EffectLayer.ts` in `rendering/pixi/layers/`

## Acceptance Criteria
- [ ] `EffectLayer.ts` created
- [ ] Area effects (puddles, epicenter, etc.) rendered
- [ ] Floating damage/heal numbers rendered with BitmapText
- [ ] Glitch cloud effects rendered
- [ ] Black hole visual effects rendered
- [ ] All effects animated correctly
- [ ] Visual parity with Canvas 2D version

## Files
- **New**: `logic/rendering/pixi/layers/EffectLayer.ts`
- **Reference**: `EffectRenderer.ts`
