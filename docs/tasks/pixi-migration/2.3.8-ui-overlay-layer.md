# Task 2.3.8 — UI Overlay Layer

## Status: Not Started

## Dependencies: 2.1

## Goal
Migrate screen-space UI overlays to PixiJS.

## Current Implementation
- Vignette — full-screen gradient overlay
- Screen pulse — full-screen tinted flash with alpha animation
- Boss indicator — directional arrow pointing to off-screen boss

## PixiJS Implementation
- Vignette -> full-screen gradient `Sprite` in uiContainer
- Screen pulse -> full-screen tinted `Sprite` with alpha animation
- Boss indicator -> `Graphics` arrow in screen-space

## Key Difference
These render in `uiContainer` (screen-space), not `worldContainer` (world-space).

## Acceptance Criteria
- [ ] `UILayer.ts` created
- [ ] Vignette effect rendered in screen-space
- [ ] Screen pulse/flash effects working
- [ ] Boss direction indicator working
- [ ] All overlays properly layered above game content

## Files
- **New**: `logic/rendering/pixi/layers/UILayer.ts`
- **Reference**: UI overlay sections from existing renderers
