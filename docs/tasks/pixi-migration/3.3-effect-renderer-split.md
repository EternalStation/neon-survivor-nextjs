# Task 3.3 — EffectRenderer Decomposition

## Status: Not Started

## Dependencies: None (can be done before or after PixiJS migration)

## Goal
Split `EffectRenderer.ts` (1,307 lines — over 800 limit) into single-responsibility modules.

## Current State
One file handles 10+ unrelated effect types: area effects, particles, floating numbers, screen effects, shields.

## Target Structure

```
rendering/renderers/
  effects/
    AreaEffectRenderer.ts    — puddles, epicenter, blackhole, glitch_cloud
    ParticleRenderer.ts      — shard, spark, bubble, vapor, void particles
    FloatingNumberRenderer.ts — damage/heal text
    ScreenEffectRenderer.ts  — vignette, pulse, screen flash
    ShieldRenderer.ts        — epicenter shield visual
```

## Acceptance Criteria
- [ ] `effects/` directory created
- [ ] `AreaEffectRenderer.ts` — area effect rendering extracted
- [ ] `ParticleRenderer.ts` — particle rendering extracted
- [ ] `FloatingNumberRenderer.ts` — floating number rendering extracted
- [ ] `ScreenEffectRenderer.ts` — screen overlay rendering extracted
- [ ] `ShieldRenderer.ts` — shield visual rendering extracted
- [ ] Original `EffectRenderer.ts` deleted or reduced to re-exports
- [ ] All files under 800 lines
- [ ] No visual regression

## Files
- **New**: 5 files in `rendering/renderers/effects/`
- **Deleted/Modified**: `EffectRenderer.ts`
