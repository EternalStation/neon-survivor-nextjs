# Task 2.3.1 — Background + Map Layer

## Status: Done

## Dependencies: 2.1

## Goal
Migrate `MapRenderer.ts` (176 lines) to PixiJS — the simplest renderer, ideal first migration.

## Current Implementation
- Grid lines drawn with `moveTo/lineTo`
- Map boundaries drawn as rectangles
- Arena vignette drawn as radial gradient

## PixiJS Implementation
- Grid lines -> `Graphics` with `moveTo/lineTo` (direct port)
- Map boundaries -> `Graphics` rectangles
- Arena vignette -> Pre-rendered gradient texture as `Sprite`

## Source File
- `MapRenderer.ts` — 176 lines

## Layer
- `BackgroundLayer.ts` in `rendering/pixi/layers/`

## Acceptance Criteria
- [x] `BackgroundLayer.ts` created
- [x] Grid lines rendered via PixiJS Graphics
- [x] Map boundaries rendered
- [ ] Vignette effect rendered via gradient texture (skipped — original implementation is empty/commented out)
- [x] Visual parity with Canvas 2D version
- [x] Layer added to worldContainer (via `container` property)

## Files
- **New**: `logic/rendering/pixi/layers/BackgroundLayer.ts`
- **Reference**: `MapRenderer.ts`
