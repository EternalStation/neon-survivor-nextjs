# Task 3.1 — Enemy Logic File Split

## Status: Not Started

## Dependencies: None (independent of rendering migration)

## Goal
Restructure the `enemies/` directory from monolithic files into single-responsibility modules.

## Current Structure (monolithic)
```
enemies/
  EnemyIndividualUpdate.ts   (759 lines) — switch on every enemy type
  UniqueEnemyLogic.ts        (774 lines) — 5 unique enemies in one file
  BossLogicPart2.ts          (643 lines) — 3 bosses in one file
```

## Target Structure
```
enemies/
  EnemyLogic.ts              — orchestrator (unchanged)
  EnemySpawnLogic.ts         — spawning (unchanged)
  EnemySystemLogic.ts        — grid, cleanup (unchanged)
  EnemyAILogic.ts            — movement utils (unchanged)
  EnemyArchetypes.ts         — shape definitions (unchanged)
  EnemyMergeLogic.ts         — merge system (unchanged)

  update/
    EnemyUpdateDispatch.ts   — dispatch to correct updater (<100 lines)
    NormalEnemyUpdate.ts     — circle, triangle, square, diamond, pentagon
    EliteEnemyUpdate.ts      — elite variants (can stay, 405 lines)

  unique/
    ZombieLogic.ts           — zombie enemy AI + behavior
    SnitchLogic.ts           — snitch enemy AI + behavior
    MinionLogic.ts           — minion/phalanx drone logic
    GlitcherLogic.ts         — glitcher enemy AI
    PrismLogic.ts            — prism enemy AI

  bosses/
    CircleBossLogic.ts       — circle boss
    DiamondBossLogic.ts      — diamond boss (extract from BossLogicPart2)
    PentagonBossLogic.ts     — pentagon boss (extract from BossLogicPart2)
    OverlordBossLogic.ts     — overlord boss (extract from BossLogicPart2)

  worm/
    WormLogic.ts             — worm system (can stay, 427 lines)
```

## Acceptance Criteria
- [ ] `update/` directory created with dispatcher + normal update
- [ ] `unique/` directory created with 5 individual logic files
- [ ] `bosses/` directory created with individual boss files
- [ ] `EnemyIndividualUpdate.ts` replaced by dispatch system
- [ ] `UniqueEnemyLogic.ts` split into individual files
- [ ] `BossLogicPart2.ts` split into individual files
- [ ] All files under 800 lines
- [ ] Game behavior unchanged

## Files
- **New**: ~12 new files in `update/`, `unique/`, `bosses/`
- **Deleted**: `EnemyIndividualUpdate.ts`, `UniqueEnemyLogic.ts`, `BossLogicPart2.ts`
- **Modified**: Files that import from deleted files
